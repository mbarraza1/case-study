# Runtime (API server + agent)
fastapi==0.115.6
uvicorn[standard]==0.34.0
anthropic>=0.40.0
pydantic>=2.0
python-dotenv==1.0.1

# Ingestion only (the scraper). Not needed to run the API.
playwright==1.60.0
