# Copy to .env and fill in your key:
#   cp .env.example .env
ANTHROPIC_API_KEY=

# Optional: comma-separated allowed origins for the frontend (defaults to CRA dev server)
# CORS_ORIGINS=http://localhost:3000
